# deepagents 框架问题

1. 文件系统后端如果设置虚拟模式，会将传入的目录路径视作文件系统的根目录，但只对 `read_file` `write_file` `edit_file` `ls` `glob` `grep` 等工具生效，对 `execute` 无效。而在文件系统中间件的提示词中明确写出指定路径时优先使用 `/xxx` 的绝对路径，导致 `execute` 的路径参数如果以`/`开始，则可能找不到文件。
2. 文件系统后端的 `read_file` 工具默认只读 100 行，可能导致模型只读了部分文件内容而将其当做文件全部内容。
