# Insight Agent 教学文档大纲

## 1. 项目介绍

### 1.1 功能介绍
- `insight-agent` 是一个面向归因分析场景的智能体应用
- 面向业务分析、经营诊断和归因定位等场景，帮助用户把“提出问题”逐步推进到“得到分析结果和可交付产物”
- 把原本需要人工查数、整理材料、反复追问和输出报告的过程整合进一次连续的智能分析体验中
- 帮助用户更快定位原因、沉淀分析过程，并形成可继续追问和复用的结果
- 附图：页面实际使用效果与一次分析任务的最终交付结果

### 1.2 核心能力
- 提供完整的会话管理能力，包括创建会话、获取历史消息、更新标题和逻辑删除
- 提供基于 WebSocket 的流式聊天能力，把模型回复、工具调用和工具结果实时返回给前端
- 基于 `deepagents` 组织 Agent 运行时，统一挂载模型、自定义工具、MCP 工具和 Skill
- 为每个用户会话分配独立工作区，用来承接上传附件、分析中间产物和最终报告文件
- 通过 `db_query` 查询业务数据，并把结果沉淀为工作区内可继续分析的文件
- 通过 `return_file` 把工作区中的分析结果文件作为附件返回给用户
- 通过消息 Schema 与 Mapper 打通前端消息、数据库记录和 Agent 运行时消息三种格式
- 支持上下文压缩与恢复，控制长对话成本，同时保持历史会话可继续追问
- 通过认证中间件、临时 WebSocket Token 和用户上下文实现用户鉴权与会话隔离

### 1.3 技术栈与依赖
- 后端：Python、FastAPI、SQLAlchemy、Redis、DeepAgents、LangChain 相关组件
- 前端：React、Vite、TypeScript、Zustand、shadcn/ui
- 基础设施：MySQL、Redis、认证服务、Data Agent、MCP 服务

### 1.4 系统架构
- 交互层：前端页面通过 HTTP 与 WebSocket 调用后端接口
- 应用层：FastAPI 应用负责路由分发、中间件处理、异常处理和静态资源托管
- 业务层：Service、Repository、Mapper、Schema 共同完成会话管理、消息持久化与协议转换
- Agent 层：`deepagents` 负责组织模型、工具、Skill、MCP 工具和工作区运行时
- 外部依赖层：认证服务、Data Agent、MCP 服务、MySQL、Redis 为系统提供鉴权、数据、扩展能力和存储支持

### 1.5 一次完整请求的生命周期
- 用户在前端发起一次会话消息，请求先经过认证、路由和上下文准备
- 后端加载历史消息、附件信息和必要的摘要上下文，并转换成 Agent 可消费的消息格式
- Agent 在 Skill 约束下结合模型推理，决定直接回复、调用内建工具，或进一步使用 MCP 工具
- 工具执行产生的数据文件、分析结果或附件被写入当前会话工作区，并通过消息流实时回传
- 本轮产生的用户消息、模型消息、工具消息和上下文压缩结果被持续写入数据库，供后续追问和恢复使用

## 2. 项目基础模块

### 2.1 项目目录规划
- 先规划 `app/`、`configs/`、`sql/`、`.deepagents/` 等核心目录
- 提前约定 `core`、`routers`、`services`、`repositories`、`utils` 等模块分层
- 明确哪些目录放业务逻辑，哪些目录放通用基础设施

### 2.2 配置文件与配置加载
- 编写 `config.yml` 和 `.env`，约定普通配置与敏感配置的分工
- 在 `app/config.py` 中统一加载配置，避免配置读取散落在各处
- 提前准备数据库、Redis、模型、MCP、认证、跨域等运行参数

### 2.3 通用工具模块
- 在 `app/utils/` 中先准备数据库、Redis、HTTP 客户端、日志、上下文等通用能力
- 让后续路由、服务、工具和中间件都复用这些基础能力
- 避免项目写到一半再回头补基础设施

### 2.4 异常体系与统一错误处理
- 在 `app/exceptions/` 中先定义 `base` 基础异常
- 按业务领域拆分认证异常、聊天异常等具体错误类型
- 通过统一的异常处理器把后端错误收敛成稳定的返回格式

### 2.5 应用入口与基础壳
- 在 `app/main.py` 中创建 FastAPI 应用
- 先挂好生命周期、中间件、路由和异常处理
- 提前把应用壳搭起来，为后续接入 Agent、数据库和聊天链路做准备

### 2.6 项目目录结构
```text
insight-agent/                项目根目录，统一组织后端、前端、配置与 Agent 资源
├── app/                      后端主目录，承载应用、Agent 和业务逻辑
│   ├── main.py               后端入口，负责创建应用、注册中间件与路由
│   ├── config.py             配置加载入口，统一解析运行配置
│   ├── core/                 Agent 核心目录，包含 Agent、MCP 和工具
│   ├── routers/              路由目录，对外提供 HTTP 与 WebSocket 接口
│   ├── services/             服务目录，组织聊天等核心业务流程
│   ├── schemas/              协议目录，定义前后端交互数据结构
│   ├── mappers/              转换目录，负责不同消息格式之间的映射
│   ├── entities/             实体目录，定义数据库实体模型
│   ├── repositories/         仓储目录，封装数据库读写逻辑
│   ├── middlewares/          中间件目录，处理认证与日志追踪
│   ├── exceptions/           异常目录，定义业务异常和统一异常处理
│   └── utils/                工具目录，封装数据库、Redis、HTTP、日志等能力
├── fd/                       前端目录，提供现成页面代码与构建资源
├── configs/                  配置目录，存放 `config.yml` 与 `.env`
├── sql/                      SQL 目录，存放数据库建表脚本
└── .deepagents/              DeepAgents 资源目录，存放 Skills 和会话工作区
```

## 3. 数据模型与消息转换

### 3.1 先定义什么表，为什么
- 会话表
- 消息表
- 上下文压缩表
- WebSocket 临时令牌表
- 这些表各自承载的业务职责

### 3.2 SQL 到实体模型的映射
- 建表 SQL 的设计思路
- `entities` 层如何承接数据库结构
- 为什么消息和附件需要以结构化 JSON 持久化

### 3.3 Repository 模式在这个项目里的价值
- 为什么不把 SQL 逻辑散落在 Service 和 Router 中
- 会话、消息、摘要、令牌仓储分别负责什么

### 3.4 为什么这个项目的 Schema 特别重要
- 它不只是 API 入参出参
- 它还承担前端消息格式、数据库持久化格式与 Agent 输入输出格式之间的桥梁角色

### 3.5 会话相关 Schema
- 创建、修改、删除、列表查询
- WebSocket Token 响应格式

### 3.6 消息相关 Schema
- `MessageSchema`
- `TextContent`
- `ImageContent`
- `ToolCallPart`
- `ToolResultPart`
- `Attachment`

### 3.7 多种消息表达的统一抽象
- 为什么消息要拆成 `parts`
- 为什么附件与内容分离
- 为什么需要 `context_seq` 与 `finish_reason`

### 3.8 这个项目里有哪三种消息格式
- 前后端交互的 Schema 消息
- 数据库存储的 Entity 消息
- Agent 运行时使用的 LangChain/DeepAgents 消息

### 3.9 `entity_to_schema` 与 `schema_to_entity`
- 消息如何落库
- 消息如何从数据库恢复给前端

### 3.10 `schema_to_langchain_message`
- 用户消息如何转换成 Agent 可理解的内容
- 文本、图片、工具调用结果如何分别处理
- 附件为什么要被拆成文档提示和图片 data URL

### 3.11 `langchain_message_to_schema` 与流式输出适配
- 模型回复如何转成前端消息
- 工具调用与工具返回如何映射为结构化片段
- `return_file` 为什么需要特殊处理附件返回

### 3.12 数据模型与聊天链路的协同
- 消息写入时机
- 会话更新时间刷新
- 历史消息回放
- 上下文压缩记录如何参与后续对话恢复
- Schema、数据库、工作区、工具返回之间如何互相配合
- 为什么 Mapper 是整个项目最关键的“翻译层”

## 4. Agent 核心：用 DeepAgents 组装归因分析智能体

### 4.1 为什么选择 `deepagents`
- 它解决了什么工程问题
- 它在本项目中的职责边界是什么

### 4.2 Agent 初始化过程
- 模型初始化
- MCP 工具装载
- 自定义工具注入
- Skill 目录挂载
- Backend 工厂注册

### 4.3 全局复用 Agent 实例的设计
- 为什么要懒加载
- 为什么要加锁
- 哪些资源是全局共享的，哪些是按会话隔离的

### 4.4 工作区机制
- 会话级 workspace 如何命名
- 为什么工具和技能都围绕工作区展开
- 工作区如何支撑文件读写、报告产出与附件回传

## 5. 工具系统：给 Agent 安上“手和脚”

### 5.1 工具设计原则
- 工具应该做什么，不应该做什么
- 为什么工具返回结构必须稳定

### 5.2 `db_query`：数据库查询工具
- 为什么数据库访问不直接放给 Agent
- 为什么通过 Data Agent 做隔离
- SSE 流式结果如何收敛为最终产物
- 查询结果为什么要落到工作区文件
- CSV/JSON 两种输出策略如何设计

### 5.3 `return_file`：文件回传工具
- 为什么返回文件要做安全校验
- 相对路径、路径逃逸、文件存在性如何处理
- 为什么返回的是结构化附件信息而不是直接传二进制

### 5.4 工具与消息链路的协同
- 工具调用如何显示到前端
- 工具返回如何持久化
- 文件类工具如何和附件系统打通

## 6. MCP 接入：把外部能力并入 Agent

### 6.1 MCP 在这个项目中的定位
- 它负责扩展哪些通用能力
- 它与内建工具、Skill 的边界怎么划分

### 6.2 `mcp.py` 的实现思路
- transport 到连接类的映射
- 多个 MCP 服务如何统一聚合
- `MultiServerMCPClient` 如何被 Agent 消费

### 6.3 MCP 与配置模块、Agent 模块的协同
- 为什么 MCP 配置要中心化
- 为什么工具列表要在 Agent 初始化时统一装配

## 7. Skill 系统：把“会做事”升级为“会做整件事”

### 7.1 什么是 Skill，为什么工具还不够
- 工具解决单步动作
- Skill 解决复杂任务的流程、规范和交付标准

### 7.2 `.deepagents/skills` 目录设计
- 文档处理类 Skill
- `insight` 归因分析 Skill
- Skill 文件如何被 Agent 发现与挂载

### 7.3 归因分析 Skill 深入讲解
- 什么时候触发归因分析模式
- 默认分析动作与分析维度
- 中间文件、HTML 报告与交付规范
- 为什么 Skill 会约束 `db_query`、pandas、工作区和报告生成方式

### 7.4 Skill 与工具、工作区、模型的协同
- Skill 如何驱动工具调用
- Skill 如何约束文件产出位置
- Skill 如何把“分析方法论”注入模型行为

## 8. 认证与中间件：把 Agent 服务接入真实业务系统

### 8.1 为什么认证要放在中间件层
- 统一处理 HTTP 接口访问控制
- 路由层只关心业务，不重复写认证逻辑

### 8.2 `auth` 中间件设计
- 哪些路径需要放行
- 哪些路径必须鉴权
- 如何调用外部认证服务做 introspection
- 如何把用户身份写入请求状态与上下文变量

### 8.3 `trace` 与日志体系
- 为什么 AI 应用更需要链路日志
- 哪些关键点需要打日志

### 8.4 认证模块与聊天链路的协同
- HTTP 请求如何得到用户身份
- WebSocket 如何通过临时 Token 继承认证结果
- 用户 ID 如何影响工作区隔离和数据隔离

## 9. 聊天链路：Router、Service 与 WebSocket 流式通信

### 9.1 为什么聊天接口要同时用 HTTP 和 WebSocket
- HTTP 负责资源管理与辅助操作
- WebSocket 负责低延迟流式会话

### 9.2 会话与消息相关接口设计
- 会话创建、更新、删除、列表
- 历史消息获取
- 附件上传与下载
- WebSocket Token 获取

### 9.3 WebSocket 聊天主流程
- 校验临时 Token
- 加载历史消息
- 应用上下文压缩结果
- 接收用户消息
- 调用 `chat_service.stream_chat`
- 流式推送模型与工具消息

### 9.4 `chat_service` 的职责拆解
- 消息落库
- 对话更新时间维护
- Agent 调用
- 流式结果处理
- 模型异常兜底
- 图片不支持场景兜底

## 10. 上下文管理：历史消息、压缩摘要与恢复机制

### 10.1 为什么长对话一定要做上下文治理
- 成本问题
- 上下文长度限制
- 归因分析场景天然容易长链路

### 10.2 运行时上下文与数据库上下文的区别
- 运行时消息数组如何变化
- 持久化消息如何完整保留

### 10.3 摘要压缩机制
- `_summarization_event` 是什么
- `cutoff_index` 和 `end_seq` 如何对应
- 为什么要同时改运行时上下文和数据库记录

### 10.4 历史恢复机制
- 新连接建立时如何加载消息
- 如何把最新摘要重新应用到历史消息上

## 11. 附件与文件工作流

### 11.1 上传文件如何进入系统
- 文件名安全处理
- 会话工作区存储
- 附件元数据返回给前端

### 11.2 附件如何进入模型上下文
- 文档附件转提示文本
- 图片附件转 data URL
- 文件缺失时如何降级提示

### 11.3 Agent 产物如何回到用户侧
- 工作区生成文件
- `return_file` 返回结构化结果
- 前端如何把它展示为附件

## 12. 前端接入：如何消费后端能力

### 12.1 前端调用后端接口的基本流程
- 获取会话列表与历史消息
- 创建会话与上传附件
- 获取 WebSocket Token
- 通过 WebSocket 发起聊天
- 接收流式消息与附件返回

### 12.2 后端如何托管前端资源
- 静态构建文件放在 `app/static/dist`
- 后端启动后直接提供静态页面
- 前端页面与后端 API、认证代理如何一起工作

### 12.3 联调时真正需要关注的点
- HTTP 与 WebSocket 调用链路
- 认证状态如何传给后端
- 消息结构、附件结构与流式响应格式

## 13. 贯穿案例：从一句“帮我分析为什么下滑了”开始

### 13.1 用户请求进入系统
- 前端如何发送消息
- 后端如何准备会话上下文

### 13.2 Agent 进入归因分析模式
- Skill 如何影响推理路径
- 工具如何被调起

### 13.3 数据查询、清洗、分析与报告输出
- `db_query` 生成原始文件
- 工作区内分析脚本处理数据
- 输出 HTML 报告与附件

### 13.4 最终结果如何回到用户
- 文字结论
- 工具痕迹
- 文件附件
- 数据持久化
